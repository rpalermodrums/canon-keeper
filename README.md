# CanonKeeper

Local-first, evidence-first companion app for maintaining canon, scenes, and continuity.

## Requirements
- Node >= 20
- pnpm

## Getting started
```bash
pnpm install
pnpm dev
```

## Scripts
- `pnpm dev` - run Electron + renderer in dev
- `pnpm lint` - lint the repo
- `pnpm typecheck` - run TypeScript checks
- `pnpm test` - run unit tests

## Repository layout
See `AGENTS.md` for the canonical architecture and constraints.
